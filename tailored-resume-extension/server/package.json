{
    "name": "agentex-server",
    "version": "4.0.0",
    "description": "Agentex LaTeX Compilation Server — multi-user PDF generation API",
    "main": "server.js",
    "scripts": {
        "start": "node server.js",
        "dev": "NODE_ENV=development node server.js",
        "test": "echo 'curl -X POST http://localhost:3000/compile -H Content-Type:application/json -d {\"latex\":\"\\\\documentclass{article}\\\\begin{document}Hello\\\\end{document}\"}' && echo 'Run the curl above to test'"
    },
    "dependencies": {
        "cors": "^2.8.5",
        "dotenv": "^16.4.5",
        "express": "^4.21.0",
        "express-rate-limit": "^7.4.0",
        "helmet": "^7.1.0",
        "morgan": "^1.10.0",
        "uuid": "^10.0.0"
    },
    "engines": {
        "node": ">=18.0.0"
    },
    "license": "MIT"
}